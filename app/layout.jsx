import './globals.css'

export const metadata = { title: 'Guardian Dashboard — AI demo' }

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head />
      <body>{children}</body>
    </html>
  )
}
